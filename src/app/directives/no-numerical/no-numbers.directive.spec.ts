import { NoNumbersDirective } from './no-numbers.directive';

describe('NoNumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new NoNumbersDirective();
    expect(directive).toBeTruthy();
  });
});
